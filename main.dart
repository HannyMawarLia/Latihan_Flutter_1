import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}
class  My App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold()
      appBar: AppBar
      (title: Text("Latihan Stateless Widget Hello Word") ,) ,
    ), //Appbar
      body: Text("Hello Word"),
    AppBar : AppBar(title: Text("Latihan Stateless Widget Hello World"),),
    body: Center(child: Text("190030731_Ni Kadek Hanny Mawar Lia Edystara")),
    ),
    );
  }
}

